{
  "name": "vpsos",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "bun --filter @vpsos/api dev",
    "dev:web": "bun --filter @vpsos/web dev",
    "lint": "bun --filter ./... run check"
  },
  "devDependencies": {
    "bun-types": "^1.3.8"
  }
}
